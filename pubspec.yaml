name: renovate
description: "A Flutter project to test integration with renovate"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.4.4 <4.0.0"
  flutter: ">=3.22.3"

dependencies:
  flutter:
    sdk: flutter
  path: ^1.8.2
  args: ^2.3.1
  cli_util: ^0.4.0
  yaml: ^3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
